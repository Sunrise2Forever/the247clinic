<% provide(:title, "Leave Message") %>
<h1>Leave Message</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2 col-xs-12">
    <%= form_for(@video_session, url: update_message_video_session_path(@video_session)) do |f| %>

      <%= f.label :message %>
      <%= f.text_area :message, rows: 8, class: 'form-control' %>

      <%= f.submit 'Submit message', class: "btn btn-primary" %>
    <% end %>
  </div>
</div>